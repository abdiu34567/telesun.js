



function testTypes() {
  /** @type {Telesun} */
  var telesun = new Telesun('YOUR_telesun_TOKEN');
  telesun.on(po)
  telesun.devHook(e, { server: re })
  telesun.handleWebhook(e)
  telesun.handleUpdate({ m })
  telesun.on("poll", (ctx, next) => {

  })


  new Telesun("token")
    .action("action", (/**@type ctx*/ctx) => {
      ctx.sendMessage({ parse_mode: "MarkdownV2" })
      ctx.getStage()
      ctx.sheet("sheet1").clear()
      ctx.sendMessage({})
      ctx.inlineQueryUpdate()
      ctx.sendPhoto({ chat_id })
      ctx.sendMessage({ chat_id, text, parse_mode: "HTML" })
      // ctx.sendMessage({chat_id,text,disable_notification})
    })
}






















